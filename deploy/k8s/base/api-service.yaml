apiVersion: v1
kind: Service
metadata:
  name: dpdp-api
  namespace: dpdp-compliance
spec:
  selector:
    app.kubernetes.io/name: dpdp-api
  ports:
    - name: http
      port: 3000
      targetPort: http

